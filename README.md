Very tiny reproduction repository for issue to run spock test from within IntelliJ.

Running `mvn clean test` works fine,
Running `SimpleTest.groovy` from within IntelliJ 2025.2 works fine.
But running `SimpleTestSpec.groovy` from within IntelliJ 2025.3 fails.
